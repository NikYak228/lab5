Актуальный способ сборки — через CMake (см. README.md).

Кратко для macOS (Apple Silicon):
export PATH="/opt/homebrew/bin:$PATH"
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build && ./build/mototest
